<apex:page controller="vfPageController_3">
    <apex:pageBlock >
        <apex:pageBlockTable value="{!accList}" var="acc">
            <apex:column value="{!acc.Id}"/>
            <apex:column value="{!acc.Name}"/>
            <apex:column value="{!acc.Rating}"/>
            <apex:column value="{!acc.Email__c}"/>
             
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>